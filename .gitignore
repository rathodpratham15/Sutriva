node_modules/
dist/
.sutriva/
# Fixtures are generated deterministically -- see scripts/generate-fixtures.sh
# and scripts/generate-eval-fixtures.ts (eval/ subdirectory). Manual test
# recordings (e.g. a real screen recording used to smoke-test transcription)
# also don't belong in git -- exclude common video containers broadly.
fixtures/videos/**/*.mp4
fixtures/videos/**/*.mov
fixtures/videos/**/*.webm
.sutriva-eval-report.json
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.*
!.env.example
.DS_Store
coverage/
.next/
# Personal/local Claude Code settings (permission allowlists etc.) -- .claude/commands
# and .claude/settings.json (if added later) are project-shared and should be committed.
.claude/settings.local.json
# /sutriva