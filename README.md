# TraceLens

**TraceLens gives coding agents temporal memory: it turns developer sessions into structured, timestamped evidence that Claude Code can query instead of guessing from source code alone.**

See `TraceLens_Master_Plan.md` for the full product and engineering plan.

**Status:** repository scaffold only. The Phase 0 vertical slice (MP4 → temporal timeline → MCP → Claude Code) is implemented on a feature branch and tracked via PR -- see the repository's pull requests for current progress.
